{
  "name": "keystone-app",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "dev": "keystone dev",
    "dev:test": "cross-env NODE_ENV=test dotenv -e .env.test -- keystone dev",
    "start": "npx prisma generate && keystone prisma migrate deploy && keystone start",
    "build": "NEXT_PRIVATE_MIN_WORKER_TIMEOUT=120000 keystone build",
    "build:production": "npm ci --omit=dev && keystone build",
    "generate": "keystone prisma migrate dev",
    "postinstall": "keystone postinstall",
    "deploy": "keystone build && npx prisma generate && keystone prisma migrate deploy && keystone start",
    "migrate": "keystone prisma migrate deploy",
    "migrate:reset": "keystone prisma migrate reset --force",
    "verify-deployment": "node scripts/verify-deployment.js",
    "prepare-render": "npm run verify-deployment && echo '✅ Proyecto listo para Render. Ve a DEPLOY_RENDER.md para instrucciones.'",
    "clean:cypress": "rm -rf cypress/screenshots cypress/videos cypress/downloads && echo '🧹 Archivos temporales de Cypress eliminados'",
    "clean:all": "npm run clean:cypress && rm -rf node_modules && echo '🧹 Limpieza completa realizada'",
    "env:setup": "npm run env:check || npm run env:create",
    "env:check": "test -f .env && echo '✅ .env existe' || exit 1",
    "env:create": "cp env.example .env && echo '✅ .env creado desde env.example - EDITA .env con tus valores'",
    "env:create-test": "cp env.test .env.test && echo '✅ .env.test creado desde env.test'",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:integration": "jest --testPathPattern=integration",
    "test:unit": "jest --testPathPattern=unit",
    "test:scenarios": "jest --testPathPattern=scenarios",
    "test:coverage": "jest --coverage",
    "test:setup": "docker compose -f docker-compose.test.yml up -d",
    "test:teardown": "docker compose -f docker-compose.test.yml down -v",
    "test:db:reset": "cross-env NODE_ENV=test dotenv -e env.test -- npx prisma migrate reset --force",
    "test:db:migrate": "cross-env NODE_ENV=test dotenv -e env.test -- npx prisma migrate dev",
    "test:db:seed": "cross-env NODE_ENV=test dotenv -e env.test -- ts-node cypress/support/seed.ts",
    "cypress:open": "cross-env NODE_ENV=test dotenv -e env.test -- cypress open",
    "cypress:run": "cross-env NODE_ENV=test dotenv -e env.test -- cypress run",
    "test:e2e": "npm run test:setup && sleep 5 && npm run test:db:migrate && npm run test:db:seed && npm run cypress:run && npm run test:teardown",
    "test:e2e:dev": "npm run test:setup && sleep 5 && npm run test:db:migrate && npm run test:db:seed && npm run cypress:open",
    "test:business-logic": "npx ts-node utils/__tests__/loan-business-logic.ts",
    "test:db-integration": "cross-env NODE_ENV=test dotenv -e env.test -- npx ts-node utils/__tests__/loan-database-integration.ts",
    "test:db-integration-safe": "cross-env NODE_ENV=test DATABASE_URL=postgresql://localhost:5432/solufacil_test npx ts-node utils/__tests__/loan-database-integration.ts"
  },
  "dependencies": {
    "@keystone-6/auth": "^8.0.0",
    "@keystone-6/core": "^6.3.1",
    "@keystone-6/fields-document": "^9.0.0",
    "@types/bcrypt": "^5.0.2",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.0.10",
    "axios": "^1.11.0",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.7.0",
    "dotenv": "^16.4.7",
    "fast-xml-parser": "^5.2.5",
    "form-data": "^4.0.4",
    "html2canvas": "^1.4.1",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "pdfkit": "^0.16.0",
    "pg": "^8.13.1",
    "react-icons": "^5.4.0",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@testing-library/jest-dom": "^6.4.2",
    "@testing-library/react": "^14.2.1",
    "@testing-library/user-event": "^14.5.2",
    "@types/html2canvas": "^1.0.0",
    "@types/jest": "^29.5.14",
    "@types/mocha": "^10.0.10",
    "@types/node-cron": "^3.0.11",
    "@types/pdfkit": "^0.13.9",
    "@types/supertest": "^6.0.2",
    "apollo-server-testing": "^2.25.4",
    "cross-env": "^7.0.3",
    "cypress": "^13.6.3",
    "dotenv-cli": "^7.3.0",
    "jest": "^29.7.0",
    "jest-environment-node": "^29.7.0",
    "jest-extended": "^4.0.2",
    "msw": "^2.2.1",
    "prisma": "5.22.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.2.6",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "resolutions": {
    "graphql": "^16.11.0"
  }
}
