/**
 * Theme para Summary New
 * 
 * Este archivo re-exporta del tema unificado para mantener compatibilidad
 * con los imports existentes.
 */

export {
  colors,
  shadows,
  radius,
  gradients,
  commonStyles,
  formatCurrency,
} from '../../styles/theme';

// Re-export adicionales que puedan necesitarse
export {
  spacing,
  typography,
  transitions,
} from '../../styles/theme';
