/**
 * Theme para Resumen Tab
 * 
 * Este archivo re-exporta del tema unificado para mantener compatibilidad
 * con los imports existentes.
 */

export {
  colors,
  shadows,
  radius,
  gradients,
  commonStyles,
  formatCurrency,
  spacing,
  typography,
  transitions,
} from '../../../styles/theme';
